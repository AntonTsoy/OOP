package fxlab.snake;

/**
 * Enum representing directions of snake in the game (RIGHT, LEFT, UP, DOWN).
 */
public enum Direction {
    RIGHT, // Right direction
    UP,
    LEFT, // Left direction
    DOWN; // Down direction
}
